[project]
name = "text-to-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rasil Baidar", email = "rasil.baidar@docorto.ai" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
text-to-task = "text_to_task:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
